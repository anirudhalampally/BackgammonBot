There is array stores the values that numbers coins in that position.

Taking the positive values as the Alice and  negative values as the bob.


I used the evaluation function to calculate effective move.

In evaluation function 
	1)pairing 
	2)unpairing
	3)killing
	4)Anchor
